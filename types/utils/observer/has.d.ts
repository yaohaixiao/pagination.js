export default has;
/**
 * 判断是否存在包含 topic 主题的订阅者信息
 * ==========================================================
 * @method has
 * @param {String} topic - （必须）主题名称
 * @returns {Boolean}
 */
declare function has(topic: string): boolean;
